const CollectionsModel = require('../model/collections')
const UserModel = require('../model/user')

exports.add = (apiId, collections, token) => {
  return new Promise((resolve, reject) => {
    
  })
}